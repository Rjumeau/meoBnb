<div class="container mx-auto mt-5 w-50 p-5 bg-white rounded-4">
  <h2>Ajoutez le logement de votre humain 😼</h2>
  <%= simple_form_for @flat do |f|%>
    <%= f.input :description %>
    <%= f.input :address %>
    <%= f.input :photo, as: :file %>
    <%= f.input :price, label: 'Price per night', input_html: { min: '0', step: 'any' } %>
    <%= f.button :submit, class: 'submit-button text-center' %>
  <% end %>
</div>
